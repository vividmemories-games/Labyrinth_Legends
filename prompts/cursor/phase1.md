# Phase 1 — MVP Foundation

Build against `docs/second-brain/00_Master_Project_Specification.md`.

## Invariants
- Game logic in `lib/game_engine/`
- UI in `lib/features/`
- Progress via `data/repositories/`
- Draw path → Go → execute

## Run
fvm flutter run --flavor dev --dart-define=FLAVOR=dev
