# Review Package: Maze Render POC — Keep Over Main After Merge

> Generated by `scripts/new_review_package.sh` (template v2).
> File: `docs/99_Reviews/Design_System/0091_maze_render_poc_main_merge.md`

## Metadata

| Field | Value |
|-------|-------|
| **Review ID** | 0091 |
| **Date** | 2026-07-09 |
| **Phase** | Keep code-drawn maze POC over main PNG tiles after merge |
| **Owner** | Cursor |
| **Status** | Ready for Review |
| **Review Type** | Design System |
| **Template Version** | v2 |
| **Related Docs** | `docs/99_Reviews/Design_System/0090_maze_render_poc.md`, `docs/99_Reviews/Architecture/0089_edge_based_maze_authored_tiles.md`, `docs/00_Project/Decisions.md` |
| **Related Screens** | `/dev/maze-render-poc` (dev), Home (dev menu), Gameplay (production PNG path unchanged) |
| **Related Components** | `MazePainter`, `WallEdgeBuilder`, `WallShapeBuilder`, `MazeTheme` (POC) |

## Task Summary

Human asked to compare this branch’s maze/wall rendering POC against `main` and keep the branch’s code-drawn approach. Merged `origin/main` into `cursor/maze-render-poc`, resolved conflicts in favor of retaining the POC renderer (`MazePainter` + jittered stone edge walls + theme skins), adapted `WallEdgeBuilder` for schema-v2 `blockedEdges` so POC levels still render after main’s edge-based engine change, renumbered the original POC review package from 0063 → 0090 (main already owned 0063), and wired the POC into main’s home `_DevToolsMenu`. Out of scope: replacing production `TileView` / authored `edge_*.png` rendering on the Gameplay screen; deleting main’s PNG tile pipeline; formal LLDL component promotion.

## Motivation

- `main` shipped schema-v2 edge-based maze + authored layered PNG tiles (0089). This branch had a parallel code-drawn wall geometry POC that Human now wants to keep.
- Without a merge, the POC sat on an outdated wall-cell engine and could not load current levels.
- Preferring the POC visually does not require ripping out production PNG wiring yet — keep both: production Gameplay uses PNGs; `/dev/maze-render-poc` keeps the preferred code-drawn look for evaluation and future swap-in.

## Files Created

```text
docs/99_Reviews/Design_System/0091_maze_render_poc_main_merge.md (this package)
```

## Files Modified

```text
lib/features/maze_render_poc/rendering/wall_edge_builder.dart — dual support: wall cells + schema-v2 blockedEdges
lib/features/maze_render_poc/data/poc_level_provider.dart — default schemaVersion 2
test/features/maze_render_poc/wall_edge_builder_test.dart — v2 blockedEdges + level_005 T-junction cases
lib/features/home/presentation/home_screen.dart — keep main UI; add Maze Render POC to _DevToolsMenu
pubspec.yaml — keep both textures/ and gameplay asset folders
docs/99_Reviews/README.md — index 0090 + 0091; Next ID 0092
docs/99_Reviews/Design_System/0090_maze_render_poc.md — renumbered from 0063 (ID collision with main)
docs/00_Project/Decisions.md — record keep-POC decision
```

## Files Deleted

```text
docs/99_Reviews/Design_System/0063_maze_render_poc.md — renamed to 0090_maze_render_poc.md
```

## Risk Assessment

| Field | Value |
|-------|-------|
| **Risk Level** | Medium |
| **Affected Areas** | Merge of main into POC branch; dual maze visual pipelines (PNG production + code-drawn POC); review ID renumber |
| **Rollback Complexity** | Medium (revert merge commit; POC folder remains deletable independently) |
| **Migration Required** | No for players; yes for POC consumers of wall-cell-only levels (now also reads blockedEdges) |
| **User-Facing Impact** | No (POC still debug-gated); production Gameplay visuals unchanged |

## Dependencies

### Depends On

- Schema v2 engine (`CellEdges`, `MazeGrid.resolvedEdgesAt`, `canTraverse`) from main / 0089
- Original maze render POC (0090 / prior commits on this branch)
- Bundled `world_1` v2 levels

### Enables

- Side-by-side evaluation of code-drawn walls vs authored PNG edges on current levels
- Future production swap of `TileView` edge overlays → `MazePainter` without another engine migration

### Blocks

- Nothing immediately; production PNG path remains authoritative for Gameplay until a follow-up swap task

## Product Impact

| Area | Impact |
|------|--------|
| **Player-facing** | No — production Gameplay still uses authored PNG edges |
| **Visual impact** | Dev POC retains preferred jittered stone / theme-skinned walls on v2 levels |
| **UX impact** | Home dev menu gains Maze Render POC entry (debug builds only) |
| **Performance impact** | Unchanged for production; POC paint cost unchanged |
| **Developer experience impact** | Branch is current with main; POC works against schema-v2 levels |

## Design System Impact

- Tokens added/changed: None
- Components added/changed: None in `design_system/` — POC remains under `features/maze_render_poc/`
- LLDL compliance notes: POC still exploratory; not promoted to LLDL components
- Showcase updated: N/A

## Gameplay Impact

- Engine changes: None (read-only use of `blockedEdges` / wall cells)
- Mechanics affected: None
- Levels affected: None for production; POC can load v2 `level_001`–`level_020` via `pocLevelProvider`

## Architecture Impact

- Layers touched: `features/maze_render_poc` (adapt), `features/home` (dev entry), docs/reviews
- New dependencies: None
- Service interfaces: None
- Note: Two visual pipelines coexist intentionally until Human approves production swap

## Documentation Updated

```text
docs/99_Reviews/Design_System/0090_maze_render_poc.md (renumber)
docs/99_Reviews/Design_System/0091_maze_render_poc_main_merge.md (this package)
docs/99_Reviews/README.md
docs/00_Project/Decisions.md
```

## LLDL Compliance Notes

- [x] Primary actions use Ancient Gold (home / POC chrome via existing LL components)
- [x] Cyan reserved for energy/paths/portals
- [ ] No hardcoded visual values in feature screens — POC painter still uses theme-local colors (acceptable for POC; promote via tokens on production swap)
- [x] No default Material styling without LLDL wrappers on Home
- [ ] Components documented in `Components.md` — deferred until production promotion

## Screenshots / Visual Evidence

| Screen | Path or description |
|--------|---------------------|
| Ancient Ruins POC | `docs/assets/maze_render_poc/maze_poc_ancient_ruins.png` |
| Amazon Jungle POC | `docs/assets/maze_render_poc/maze_poc_amazon_jungle.png` |

> Re-verify on simulator after hot restart: Home → Dev Tools → Maze Render POC, especially `level_005` (v2 T-junction).

## Commands Run

```bash
git merge origin/main
fvm flutter analyze lib/features/maze_render_poc/ lib/features/home/presentation/home_screen.dart
fvm flutter test test/features/maze_render_poc/
./scripts/new_review_package.sh Design_System maze_render_poc_main_merge --phase "..." --update-index
```

## Test / Analyze Results

| Command | Result |
|---------|--------|
| `flutter test test/features/maze_render_poc/` | 11 passed |
| `flutter analyze` (POC + home) | No issues found |

## Known Issues

- Production Gameplay still renders authored PNG edges; POC is not yet the production board renderer
- `WallShapeBuilder` (block style) still keys off `CellType.wall`; v2 levels with few/no wall cells look empty in **block** wall style — **edge** style (default) is correct for v2
- Stash `stash@{0}` (`level_repository` WIP) left in place; likely obsolete after main’s `AssetManifest` fix — Human should confirm drop

## Open Questions

- Should production Gameplay swap to `MazePainter` next, or keep PNG edges through M2 polish?
- Keep or delete authored `edge_*.png` assets once code-drawn walls are production?

## Future Technical Debt

### Known Shortcuts

- Dual visual pipelines (PNG `TileView` + code-drawn POC) until a deliberate swap
- POC theme colors not yet design tokens

### Temporary Implementations

- Dev-only route and home menu entry for POC evaluation

### Future Cleanup Tasks

- Promote `MazePainter` / `MazeTheme` into `design_system/` when adopted for Gameplay
- Remove or archive PNG edge overlays if code-drawn becomes authoritative
- Cache wall path geometry for larger grids

## Recommended Next Task

Wire `MazePainter` (edge style) into production `BoardRenderer` / `TileView` as an opt-in or replacement for authored edge PNG overlays, behind a debug flag first — then decide asset retirement.

## Cursor Self-Assessment

- Scope respected: Yes — merge + keep POC; no production Gameplay renderer swap
- Docs updated: Yes — review packages 0090/0091, Decisions.md, index
- Tests adequate: Yes — edge builder covers legacy walls + v2 blockedEdges
- Ready for external review: Yes

## Ready For

- [x] Codex Engineering Review
- [x] Human Approval

---

## Reviewer Notes

### Codex Engineering Review

- **Date:**
- **Verdict:**
- **Score:**
- **Blocking Issues:**
- **Recommendations:**

### Human Approval

- **Date:**
- **Verdict:**
- **Notes:**
