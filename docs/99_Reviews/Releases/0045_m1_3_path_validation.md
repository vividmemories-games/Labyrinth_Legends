# Review Package: M1.3 Path Validation

> Generated by `scripts/new_review_package.sh` (template v2).
> File: `docs/99_Reviews/Releases/0045_m1_3_path_validation.md`

## Metadata

| Field | Value |
|-------|-------|
| **Review ID** | 0045 |
| **Date** | 2026-07-03 |
| **Phase** | M1.3 — Path Validation |
| **Owner** | Cursor |
| **Status** | Approved & Locked |
| **Review Type** | Release |
| **Template Version** | v2 |
| **Related Docs** | `Engine_Architecture.md` · `Coding_Standards.md` · `Level_Format.md` · GP2 · GP7 · `0043` · `0044` |
| **Related Screens** | N/A — engine only |
| **Related Components** | N/A |

## Task Summary

Implemented production `PathValidator` with typed `PathValidationError` / `PathValidationErrorCode` results, integrated validation into `GameplaySession.confirmPath()` and `validateDraftPath()`. Rebuilt prototype validator to be stateless (`const PathValidator`) with PV rule IDs per `Level_Format.md` §16. **Out of scope:** execution, tile interactions, UI/provider rewrite, save/progress.

## Motivation

**Why:** M1.3 gates player commitment — invalid paths must be rejected before execution per GP2-L03 and GP7 §5.

**Problem solved:** M1.2 `confirmPath()` accepted any draft path; prototype validator used untyped string messages only.

**Approach:** Stateless `PathValidator` reports facts; `GameplaySession` orchestrates confirm, owns phase transitions, sets `lifecycleMessage` on failure.

## Files Created

```text
lib/game_engine/path/path_validation_error.dart
docs/99_Reviews/Releases/0045_m1_3_path_validation.md
```

## Files Modified

```text
lib/game_engine/path/path_validator.dart — typed PV rules, stateless, GP2-L09 revisits
lib/game_engine/path/path_validation_result.dart — typed errors list + errorCode
lib/game_engine/session/gameplay_session.dart — PathValidator injection, confirmPath integration
test/game_engine/path_validator_test.dart — expanded PV coverage
test/game_engine/gameplay_session_test.dart — valid exit paths, confirm validation tests
lib/game_engine/session/gameplay_session.dart — canConfirmPath doc comment (Codex follow-up)
test/game_engine/path_validator_test.dart — errorCode disambiguation tests (Codex follow-up)
```

## Files Deleted

```text
(none)
```

## Risk Assessment

| Field | Value |
|-------|-------|
| **Risk Level** | Medium |
| **Affected Areas** | Path confirm flow, gameplay provider `go()` via `confirmPath()` |
| **Rollback Complexity** | Low |
| **Migration Required** | No — API additive (`validateDraftPath`, typed errors) |
| **User-Facing Impact** | Indirect — invalid paths now blocked at confirm (correct behaviour) |

## Dependencies

### Depends On

- M1.2 `GameplaySession` skeleton (0044)
- M1.1 `LevelDefinition` (0043)
- `Engine_Architecture.md` EA-001, EA-005, EA-008

### Enables

- M1.4+ execution (`advanceExecutionStep`, PathExecutor)
- `test/game_engine/level_001_test.dart` confirm-gated gameplay path
- Provider/UI wiring for GP6 validation feedback

### Blocks

- M1.4+ until Human approval

## Product Impact

| Area | Impact |
|------|--------|
| **Player-facing** | Indirect — confirm now enforces GP2 legality |
| **Developer experience impact** | High — typed PV errors, testable validation |

## Design System Impact

- N/A

## Gameplay Impact

- Validation only — no execution or collection
- `reachExit` objective drives `requireExit` on confirm

## Architecture Impact

- Validation logic lives only in `PathValidator` (Coding_Standards §6)
- `GameplaySession` delegates to validator; owns confirm outcome (EA-008)
- `PathValidator` is engine-internal, stateless, injectable for tests
- No Flutter imports in `game_engine/path/`

## Validation Rules Implemented

| ID | Rule | Authority |
|----|------|-----------|
| PV-01 | Path non-empty | GP2 · Level_Format §16 |
| PV-02 | Path starts at start cell; level has start | GP2 |
| PV-03 | Path ends at exit when `requireExit` (from `objectives.reachExit`) | GP2 · GP5 |
| PV-04 | Orthogonally adjacent steps only; in-bounds | GP2-L02 |
| PV-06 | No wall traversal; no `hidden` visibility cells | GP2 |
| PV-07 | Locked cells only after matching key collected along prefix | GP3.3 · GP7 |

**Revisits:** Permitted per GP2-L09 (prototype revisit rejection removed).

**Not implemented (deferred):** fog/relic-gated discovery grids, hazards, gem collection requirements, ice/conveyor modifiers, continuous draft-time validation in `appendDraftPosition`.

## Documentation Updated

```text
docs/99_Reviews/Releases/0045_m1_3_path_validation.md
docs/99_Reviews/README.md
```

## LLDL Compliance Notes

- [x] N/A — no UI

## Commands Run

```bash
flutter test
flutter test test/game_engine/path_validator_test.dart
flutter test test/game_engine/gameplay_session_test.dart
flutter analyze --no-fatal-infos lib/game_engine/path/ lib/game_engine/session/gameplay_session.dart
```

## Test / Analyze Results

| Command | Result |
|---------|--------|
| `flutter test` | 90 passed |
| `flutter test test/game_engine/path_validator_test.dart` | 16 passed |
| `flutter test test/game_engine/gameplay_session_test.dart` | 18 passed |
| `flutter analyze` (changed files) | 0 errors, 0 warnings |

## Known Limitations

- `appendDraftPosition` does not validate incrementally (draft can contain invalid segments until confirm)
- Validation uses static `level.grid` — discovery/fog visibility mutations not applied (stretch 004+)
- `hiddenUntilRelicId` gating not validated at path-confirm time
- No `level_001.json` asset path fixture test yet (deferred to M1.4 gameplay path test)

## Architecture Note — Revisit validation (GP2-L09)

Per AGENTS.md documentation hierarchy, **GP2-L09** (revisits allowed when path includes them) takes precedence over `Level_Format.md` §16 PV-05 legacy wording ("no duplicate revisit"). Implementation correctly follows GP2. A future `Level_Format.md` cleanup should align PV-05 with GP2-L09 and report via review package.

## Open Questions

- None

## Future Technical Debt

- Incremental draft validation for GP6 live feedback during drawing
- Apply discovery-engine visibility to validator input grid for fog/relic levels
- `test/game_engine/level_001_test.dart` with real asset + shortest valid path
- Map `PathValidationError` to GP6 player-facing copy (not raw engine strings)
- Preserve `PathValidationErrorCode` enum stability as the engine validation contract for UI feedback
- Use `validateDraftPath().isValid` (not `canConfirmPath`) for Go-button readiness in provider rewrite

## Recommended Next Task

**M1.4 — Execution skeleton:** implement `advanceExecutionStep()` with `PathExecutor` → `StepResolver` stub, one-tile advance, no win/loss yet; add `level_001` gameplay path test.

## Cursor Self-Assessment

- Scope respected: Yes — validation only, no execution
- Docs updated: Review package; GP2/Level_Format conflict reported in Open Questions
- Tests adequate: Yes — 16 validator tests (incl. errorCode disambiguation)
- Ready for external review: Yes

## Ready For

- [x] Codex Engineering Review
- [x] ChatGPT Product Review
- [x] Human Approval

---

## Codex Follow-Up Resolution (2026-07-03)

| Issue | Resolution |
|-------|------------|
| **Blocking:** `errorCode` inferred from non-unique `ruleId` | `PathValidationError.code` is authoritative; `ruleId` derived from `code`; `PathValidationResult.errorCode` reads `firstError?.code` |
| **Recommendation:** `canConfirmPath` semantics | Doc comment clarifies non-empty draft ≠ valid draft; use `validateDraftPath()` for readiness |
| **Recommendation:** Disambiguation tests | Added 6 tests in `errorCode disambiguation` group |

---

## Reviewer Notes

### Codex Engineering Review

- **Date:** 2026-07-03
- **Verdict:** Changes requested
- **Score:** 8/10
- **Blocking Issues:**
  - `lib/game_engine/path/path_validation_result.dart:27` reconstructs `errorCode` from `firstError.ruleId`, but several `PathValidationErrorCode` values intentionally share the same PV rule ID (`PV-02`, `PV-03`, `PV-04`, `PV-06`). This means typed callers can receive the wrong code: `notAtStart` resolves as `missingStartCell`, `notAdjacent` resolves as `outOfBounds`, `hiddenCell` resolves as `notWalkable`, and `notAtExit` resolves as `missingExitCell`. Because this review package explicitly introduces typed path validation errors, the concrete typed code must be preserved on `PathValidationError` rather than inferred from a non-unique rule ID, with tests for the ambiguous pairs.
- **Recommendations:**
  - Keep the GP2-L09 revisit behavior as implemented. The review package correctly reports the lower-authority `Level_Format.md` PV-05 conflict in Open Questions, and the implementation follows the Core Gameplay Specification.
  - Add a small assertion around `GameplaySession.canConfirmPath` semantics in the next pass. It currently means “has a non-empty draft in drawing phase,” not “current draft is valid.” That is acceptable for confirm-gated M1.3, but any UI that uses it to enable a Go button will need either `validateDraftPath()` or a renamed/clearer readiness contract before GP6 live feedback work.
  - Verification performed: `flutter test test/game_engine/path_validator_test.dart` passed, `flutter test test/game_engine/gameplay_session_test.dart` passed, and `flutter analyze --no-fatal-infos lib/game_engine/path/ lib/game_engine/session/gameplay_session.dart` passed. Initial parallel test execution hit a Flutter generated native-assets race; rerunning the affected focused test alone passed.

### Codex Re-Review (post follow-up)

- **Date:** 2026-07-03
- **Verdict:** ✅ Approved
- **Score:** 9.2 / 10
- **Notes:** `PathValidationError.code` is now authoritative; `ruleId` is derived. Six disambiguation tests cover ambiguous PV ID pairs. `canConfirmPath` documented. Ready for Human approval.

### ChatGPT Product / Architecture Review

- **Date:** 2026-07-03
- **Verdict:** 🟢 Approved & Locked
- **Score:** 9.8 / 10

**Product Notes:**

M1.3 successfully introduces stateless path validation while preserving the architectural boundaries established in Engine_Architecture.md.

`GameplaySession` remains the single orchestration point, `PathValidator` remains stateless, and gameplay execution is correctly deferred to M1.4.

The implementation demonstrates excellent scope discipline and maintains the Pure Dart engine boundary.

No conflicts were identified with Vision, Gameplay, Coding_Standards.md, Engine_Architecture.md, or AGENTS.md.

**Architecture Notes:**

- EA-001 ✔
- EA-002 ✔
- EA-003 ✔
- EA-004 ✔
- EA-005 ✔
- EA-006 ✔
- EA-007 ✔
- EA-008 ✔

The only documentation inconsistency concerns revisit validation. GP2-L09 is the authoritative gameplay specification and therefore correctly takes precedence over the legacy wording in Level_Format.md. The implementation is correct; the documentation should be aligned in a future cleanup.

**Recommendations (Non-blocking):**

- Replace the current Open Question with an Architecture Note explaining the documentation hierarchy and why GP2-L09 is authoritative.
- Add a future technical debt item to preserve `PathValidationErrorCode` stability as the engine's validation contract for future UI feedback.

**Recommendation:**

Approve, lock M1.3, and proceed to **M1.4 – Execution Pipeline**.

### ChatGPT Re-Review (post follow-up)

- **Date:** 2026-07-03
- **Verdict:** ✅ Approved & Locked
- **Score:** 9.9 / 10
- **Notes:** Codex typed-error fix satisfies validation contract stability. GP2-L09 architecture note added per recommendation. Ready for Human approval.

### Human Approval

- **Date:** 2026-07-03
- **Verdict:** Approved
- **Notes:**
